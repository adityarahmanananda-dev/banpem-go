services:
  app:
    build: ./backend
    environment:
      DATABASE_URL: ${DATABASE_URL:?Set DATABASE_URL di file .env}
      BACKUP_DIR: /backups
      ADDR: :8080
    ports:
      - "8080:8080"
    volumes:
      - backups:/backups

volumes:
  backups:
